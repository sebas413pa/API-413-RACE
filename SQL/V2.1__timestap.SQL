use `413_race`;
-- 15. PURCHASES
ALTER TABLE purchases
ADD COLUMN status BOOLEAN DEFAULT TRUE AFTER total,
ADD COLUMN createdAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
ADD COLUMN updatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP;

-- 16. PURCHASE_DETAILS
ALTER TABLE purchase_details
ADD COLUMN status BOOLEAN DEFAULT TRUE AFTER subtotal,
ADD COLUMN createdAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
ADD COLUMN updatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP;

-- 17. BATCHES
ALTER TABLE batches
ADD COLUMN status BOOLEAN DEFAULT TRUE AFTER available_qty;

-- 18. QUOTATIONS
ALTER TABLE quotations
ADD COLUMN is_active BOOLEAN DEFAULT TRUE AFTER total;

-- 20. SALE_DETAILS
ALTER TABLE sale_details
ADD COLUMN status BOOLEAN DEFAULT TRUE AFTER subtotal,
ADD COLUMN createdAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
ADD COLUMN updatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP;

-- 21. TOKENS
ALTER TABLE tokens
ADD COLUMN status BOOLEAN DEFAULT TRUE AFTER expires_at;

-- 22. PAYMENTS
-- 24. PROMOTION_PRODUCTS
ALTER TABLE promotion_products
ADD COLUMN status BOOLEAN DEFAULT TRUE AFTER car_id;

-- 26. PROMO_CODE_USES
ALTER TABLE promo_code_uses
ADD COLUMN status BOOLEAN DEFAULT TRUE AFTER discount_amount;
